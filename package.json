{
    "name": "agent-descent",
    "version": "0.1.0",
    "type": "module",
    "bin": {
        "agent-descent": "./bin/agent-descent.js"
    },
    "files": [
        "bin/",
        "dist/"
    ],
    "scripts": {
        "start": "tsx src/index.ts",
        "build": "tsc && node -e \"require('fs').cpSync('src/agents/prompts','dist/agents/prompts',{recursive:true})\"",
        "lint": "tsc --noEmit"
    },
    "dependencies": {
        "@github/copilot-sdk": "^0.2.1",
        "zod": "^4.3.6"
    },
    "devDependencies": {
        "tsx": "^4.19.0",
        "typescript": "^5.7.0",
        "@types/node": "^22.0.0"
    },
    "engines": {
        "node": ">=20.6.0"
    }
}
